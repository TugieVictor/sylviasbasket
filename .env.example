# Development Server Configuration
PORT=3001

# Contentful CMS Configuration
# Get these from: https://app.contentful.com/ → Space Settings → API Keys
# NOTE: These use NEXT_PUBLIC_ prefix because they're accessed client-side
# This is acceptable for read-only public content access
NEXT_PUBLIC_CONTENTFUL_SPACE_ID=your_contentful_space_id_here
NEXT_PUBLIC_CONTENTFUL_ACCESS_TOKEN=your_contentful_access_token_here

# Contentful Preview API (Optional - for draft content preview)
CONTENTFUL_PREVIEW_ACCESS_TOKEN=your_contentful_preview_token_here

# Mailchimp Email Marketing Configuration
# Get these from: https://mailchimp.com/ → Account → Extras → API keys
# IMPORTANT: These should NOT have NEXT_PUBLIC_ prefix (server-side only)
# Never expose these keys to the client-side code
MAILCHIMP_AUDIENCE_ID=your_mailchimp_audience_id_here
MAILCHIMP_API_KEY=your_mailchimp_api_key_here
MAILCHIMP_SERVER_PREFIX=us1

# Resend Email Service Configuration (for contact form and donation receipts)
# Get this from: https://resend.com/api-keys
# IMPORTANT: Server-side only - do NOT use NEXT_PUBLIC_ prefix
# Free tier: 3,000 emails/month (more than sufficient)
# Verify domain: donations@sylviasbasket.co.ke
RESEND_API_KEY=re_your_resend_api_key_here

# Donation System Configuration
# iPay Africa Payment Gateway (PENDING CREDENTIALS FROM CLIENT)
IPAY_VENDOR_ID=your_ipay_vendor_id
IPAY_HASH_KEY=your_ipay_hash_key
IPAY_CALLBACK_URL=https://sylviasbasket.co.ke/api/donations/callback
IPAY_MODE=test

# Bank Transfer Details (for manual donations)
NEXT_PUBLIC_BANK_NAME=Equity Bank
NEXT_PUBLIC_BANK_ACCOUNT_NAME=Sylvia's Basket
NEXT_PUBLIC_BANK_ACCOUNT_NUMBER=your_account_number

# Database (PostgreSQL) - Required for donation system
# Use Neon (neon.tech), Supabase (supabase.com), or local PostgreSQL
# See PRISMA_SETUP.md for detailed setup instructions
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Site URL (for callbacks and redirects)
NEXT_PUBLIC_SITE_URL=https://sylviasbasket.co.ke

# Security Notes:
# 1. Never commit .env.local to git (already in .gitignore)
# 2. Rotate API keys immediately if they are accidentally exposed
# 3. Use different keys for development vs production environments
# 4. NEXT_PUBLIC_ prefix exposes variables to client-side - use sparingly
# 5. Server-side API keys (Mailchimp) must never use NEXT_PUBLIC_ prefix
